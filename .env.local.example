DATABASE_URL=postgresql://app:password@localhost:5432/appdb
SESSION_SECRET=change-me-to-a-long-random-string
# Override Cypress baseUrl. Not needed in Replit (REPLIT_DEV_DOMAIN is auto-detected)
# or locally (falls back to http://localhost:5000). Only set this if you need a
# different URL (e.g. a non-default port or a staging server).
# CYPRESS_BASE_URL=http://localhost:5000
